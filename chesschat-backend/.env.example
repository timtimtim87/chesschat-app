# Railway Environment Variables
# Copy this to your Railway project environment variables

# Node environment
NODE_ENV=production

# Server port (Railway will provide this automatically)
PORT=3001

# PostgreSQL Database URL (Railway will provide this when you add PostgreSQL)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/chesschat

# CORS Origins - Add your frontend URLs here
CORS_ORIGINS=https://chesschat-web.vercel.app,https://your-frontend-domain.com

# Optional: Custom database connection settings
DB_MAX_CONNECTIONS=10
DB_TIMEOUT=30000

# Security settings
TRUST_PROXY=true
